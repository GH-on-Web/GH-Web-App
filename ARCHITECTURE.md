# NodeParser Architecture Diagram

## Component Structure

```
┌─────────────────────────────────────────────────────────────┐
│                    NodeParserDemo.js                        │
│                     (Demo Page)                             │
│  ┌───────────────┐  ┌─────────────┐  ┌─────────────────┐  │
│  │  Load/Export  │  │  JSON View  │  │  Connection     │  │
│  │   Controls    │  │             │  │     List        │  │
│  └───────────────┘  └─────────────┘  └─────────────────┘  │
│                                                             │
│              ┌───────────────────────┐                     │
│              │   State Management    │                     │
│              │  - graphData          │                     │
│              │  - components DB      │                     │
│              │  - connections        │                     │
│              └───────────────────────┘                     │
│                         │                                   │
└─────────────────────────┼───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    NodeParser.js                            │
│                   (Main Component)                          │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │               React Flow Canvas                       │ │
│  │                                                       │ │
│  │  ┌────────────┐   ┌────────────┐   ┌────────────┐  │ │
│  │  │ GH Node 1  │───│ GH Node 2  │   │ GH Node 3  │  │ │
│  │  │  [I] [O]   │   │  [I] [O]   │   │  [I] [O]   │  │ │
│  │  └────────────┘   └────────────┘   └────────────┘  │ │
│  │                                                       │ │
│  │  ┌─────────────────────────┐                         │ │
│  │  │   MiniMap + Controls    │                         │ │
│  │  └─────────────────────────┘                         │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │              ComponentSearch.js                       │ │
│  │   [Search: "number slider"]                    🔍     │ │
│  │   ┌────────────────────────────────────────────────┐ │ │
│  │   │ ✓ Number Slider (Params > Input)              │ │ │
│  │   │   Number (Params > Primitive)                  │ │ │
│  │   │   Addition (Maths > Operators)                 │ │ │
│  │   └────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      User Actions                           │
└───────────┬─────────────────────────────────────────────────┘
            │
            ├─► Add Component (from search)
            ├─► Delete Node (Delete key)
            ├─► Move Node (drag)
            ├─► Create Connection (drag output to input)
            └─► Delete Connection (Ctrl+drag or Delete)
            │
            ▼
┌─────────────────────────────────────────────────────────────┐
│                    NodeParser Component                     │
│                                                             │
│  Event Handlers:                                            │
│  - onComponentSelect()                                      │
│  - onNodesDelete()                                          │
│  - onNodeDragStop()                                         │
│  - onConnect()                                              │
│  - onEdgesDelete()                                          │
└───────────┬─────────────────────────────────────────────────┘
            │
            ├─► onNodesChange(nodes, newInstance, deleted, posUpdate)
            └─► onConnectionsChange(connections)
            │
            ▼
┌─────────────────────────────────────────────────────────────┐
│              Parent Component (Demo Page)                   │
│                                                             │
│  State Update Logic:                                        │
│  - Add new instance to componentInstances[]                 │
│  - Remove deleted nodes                                     │
│  - Update node positions                                    │
│  - Update connections[]                                     │
└───────────┬─────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────┐
│                   Updated Graph Data                        │
│                                                             │
│  {                                                          │
│    componentInstances: [...],                               │
│    connections: [...]                                       │
│  }                                                          │
└───────────┬─────────────────────────────────────────────────┘
            │
            ▼ (Props update triggers re-render)
┌─────────────────────────────────────────────────────────────┐
│              NodeParser Re-renders                          │
│              with Updated Data                              │
└─────────────────────────────────────────────────────────────┘
```

## File Dependencies

```
NodeParserDemo.js
    │
    ├─► import { NodeParser } from '../components/NodeParser'
    │
    └─► import exampleData from '../data/exampleGraph.json'

NodeParser/index.js
    │
    ├─► export NodeParser from './NodeParser.js'
    ├─► export GrasshopperNode from './GrasshopperNode.js'
    ├─► export ComponentSearch from './ComponentSearch.js'
    ├─► export { parseGrasshopperGraph } from '../../utils/nodeParser.js'
    └─► export { ConnectionManager } from '../../utils/connectionManager.js'

NodeParser.js
    │
    ├─► import { GrasshopperNode } from './GrasshopperNode.js'
    ├─► import { ComponentSearch } from './ComponentSearch.js'
    ├─► import { parseGrasshopperGraph } from '../../utils/nodeParser.js'
    ├─► import { ConnectionManager } from '../../utils/connectionManager.js'
    └─► import ReactFlow from 'reactflow'

utils/nodeParser.js
    │
    └─► Pure functions (no dependencies)

utils/connectionManager.js
    │
    └─► Pure functions and class (no dependencies)
```

## State Management Flow

```
┌──────────────────────────────────────────────────────────────┐
│               Component State Hierarchy                      │
└──────────────────────────────────────────────────────────────┘

NodeParserDemo (Parent)
│
├─ State:
│  ├─ graphData: { componentInstances, connections }
│  ├─ componentsDatabase: Component[]
│  ├─ jsonInput: string
│  └─ currentData: GraphData
│
└─► NodeParser (Child)
    │
    ├─ Props (from parent):
    │  ├─ graphData
    │  ├─ componentsDatabase
    │  ├─ onConnectionsChange
    │  └─ onNodesChange
    │
    ├─ Internal State:
    │  ├─ nodes (React Flow)
    │  ├─ edges (React Flow)
    │  ├─ connectionManager (ref)
    │  └─ isCtrlPressed (ref)
    │
    └─► Children:
        ├─ GrasshopperNode (for each node)
        └─ ComponentSearch
```

## Callback Flow

```
User Action
    │
    ▼
┌─────────────────────────┐
│   NodeParser Component  │
│   (Internal handling)   │
└───────────┬─────────────┘
            │
            ├─ Update internal state (nodes, edges, refs)
            │
            ▼
┌─────────────────────────┐
│   Trigger Callback      │
│   to Parent             │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────────────────────────┐
│   Parent Component                          │
│   (NodeParserDemo)                          │
│                                             │
│   handleNodesChange(                        │
│     newNodes,          // Updated node list │
│     newInstance,       // New component     │
│     deletedIds,        // Deleted node IDs  │
│     isPositionUpdate   // Position change   │
│   )                                         │
│                                             │
│   OR                                        │
│                                             │
│   handleConnectionsChange(                  │
│     newConnections     // Updated edges     │
│   )                                         │
└───────────┬─────────────────────────────────┘
            │
            ▼
┌─────────────────────────┐
│   Update State          │
│   - graphData           │
│   - currentData         │
│   - jsonInput           │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│   Re-render with        │
│   Updated Props         │
└─────────────────────────┘
```

## Import/Export Flow

```
EXPORT:
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│  User       │───▶│  Export      │───▶│  Download   │
│  Clicks     │    │  graphData   │    │  JSON File  │
│  Export     │    │  as JSON     │    │             │
└─────────────┘    └──────────────┘    └─────────────┘

IMPORT:
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│  User       │───▶│  File        │───▶│  Parse      │
│  Selects    │    │  Reader      │    │  JSON       │
│  File       │    │              │    │             │
└─────────────┘    └──────────────┘    └──────┬──────┘
                                              │
                                              ▼
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│  NodeParser │◀───│  setGraphData│◀───│  Validate   │
│  Re-renders │    │  (newData)   │    │  Structure  │
│  with Data  │    │              │    │             │
└─────────────┘    └──────────────┘    └─────────────┘
```

## Component Lifecycle

```
1. MOUNT
   ├─ Load components database from /public
   ├─ Initialize empty graph data
   ├─ Set up event listeners (keyboard)
   └─ Render empty canvas

2. ADD COMPONENT
   ├─ User searches and selects
   ├─ Create new instance with unique ID
   ├─ Add to nodes state
   ├─ Call onNodesChange(nodes, newInstance)
   └─ Parent updates graphData

3. CREATE CONNECTION
   ├─ User drags from output to input
   ├─ Add to edges state
   ├─ Add to connectionManager
   ├─ Call onConnectionsChange(connections)
   └─ Parent updates graphData

4. MOVE NODE
   ├─ User drags node
   ├─ onNodeDragStop fires
   ├─ Call onNodesChange(nodes, null, null, true)
   └─ Parent updates positions in graphData

5. DELETE NODE
   ├─ User presses Delete key
   ├─ Remove from nodes state
   ├─ Remove connections from connectionManager
   ├─ Call onNodesChange(null, null, deletedIds)
   └─ Parent filters componentInstances

6. EXPORT
   ├─ User clicks export
   ├─ Serialize graphData to JSON
   ├─ Create blob and download link
   └─ Trigger download

7. UNMOUNT
   └─ Clean up event listeners
```

This architecture ensures clean separation of concerns and makes the component highly reusable!
